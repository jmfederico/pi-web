---
"@jmfederico/pi-web": patch
---

Give the Add project folder suggestion list a persistent thin scrollbar (`scrollbar-width: thin`, the pattern the other scrollable strips use) and contain its overscroll, so a list clipped by the dialog height reads as scrollable at rest instead of silently hiding rows below the fold.
