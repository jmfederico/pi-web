---
"@jmfederico/pi-web": patch
---

Render the chat activity status pill in its own row below the transcript instead of as an absolutely positioned overlay, so it no longer covers message text when the view is scrolled away from the end; when pinned to the bottom, the transcript re-scrolls as the pill appears or disappears.
