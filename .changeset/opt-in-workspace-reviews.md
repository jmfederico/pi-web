---
"@jmfederico/pi-web": patch
---

Replace the session-bridge greeting example with an opt-in Workspace Reviews Pi package. Review uncommitted workspace changes in a dedicated session, save correlated findings or failure status in plugin-owned storage, and browse saved reviews as plain text. The package is not installed by default.
