---
"@jmfederico/pi-web": patch
---

Add optional OMP sessions alongside Pi sessions. Choose the backend when creating a session, while keeping Pi as the default and preserving each backend's own credentials, configuration, and session storage. Configure the OMP executable and profile through the machine's global configuration; Pi-only installations do not require OMP or Bun.
